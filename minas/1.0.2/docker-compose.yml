services:
  minas:
    container_name: ${CONTAINER_NAME}
    restart: always
    ports:
      - "${PANEL_APP_PORT_HTTP}:8003"
    # network_mode: host
    volumes:
      - ${DATA_DIR}:/app/data
    environment:
      - TZ=Asia/Shanghai
    image: sorc/minas:1.0.2
    labels:
      createdBy: "Apps"